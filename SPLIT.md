以下是一套静态规则，用于从用户提供的杂乱标题列表中提取“按作品整理、章节列表、缺失、是否完本”等信息。规则不依赖代码，仅描述逻辑。

---

### 一、标题解析规则

每个标题依次处理：

1. **提取作者**  
   - 匹配方括号内的内容：`[作者名]`。若无方括号，作者留空（但本例中大部分有作者）。  
   - 作者名保留原样（如 `ryota tanaka`、`zerone`、`红姬`、`komachan`）。

2. **移除作者**，得到剩余字符串 `S`。

3. **分离作品名与章节信息**  
   - 在 `S` 中找到**第一个**以下模式出现的位置作为分界点：  
     - 数字（包括 `\d+`、`\d+-\d+`、`\d+-\d+-\d+`）  
     - 中文数字（一、二、…、十、百）  
     - “第X话”、“第X回”、“话X”等  
     - “S2”、“第二部”、“续集”等（这些通常与作品名结合，但若后跟数字则视为章节的开始）  
   - 分界点之前的连续字符（去除末尾标点/空格）为**作品名**。  
   - 分界点之后（包含该模式）为**章节信息串**。  
   - 若 `S` 中无任何上述模式，则整个 `S` 为作品名，章节信息为空（视为单篇无编号）。

4. **处理作品名中的子系列**  
   - 作品名中若包含连字符（`-`）、冒号、中文破折号或括号，则保留完整作为作品名。  
   - 例如 `青鸾殇-秦璐篇`、`逆转S2`、`黄蓉襄阳后记·续集` 均视为独立作品名。

5. **解析章节信息串**  
   - 从信息串中提取所有**数字范围**和**单个数**：  
     - 匹配 `\d+` 或 `\d+-\d+`（如 `1-8`）或 `\d+-\d+-\d+`（少见）。  
     - 匹配中文数字转换：一→1、二→2……十→10（十一→11 等）。  
     - 匹配 `第X话` → X。  
   - 范围展开：`a-b` 生成 a, a+1, …, b。  
   - 若出现 `+番外` 或 `+特别篇`，标记存在番外（不纳入数字章节）。  
   - 若出现 `S2 07`，提取 `07` 为数字 7（`S2` 已作为作品名一部分）。  
   - 若存在 `第二部03`，且作品名不含“第二部”，则将“第二部”合并入作品名，`03` 作为章节号。  
   - 无任何数字 → 章节列表为空（单篇）。

6. **提取完本标记**  
   - 在标题中查找以下关键词（中英文）：  
     - `完本`、`全篇`、`END`、`(完)`、`完`、`完全版`、`全本`  
   - 若有，标记该条目为完本。  
   - 注意：`1-8(完)` 形式中 `(完)` 表示范围 1-8 完整。

---

### 二、作品分组与去重

- 按 **作者 + 作品名** 作为唯一键分组。  
- 同一分组内，每个标题解析出的**章节号集合**合并（去重）。  
- 若同一章节号出现多次，只计一次。  
- 注意区分：`逆转S2` 与 `逆转` 为不同作品；`青鸾殇-秦璐篇` 与 `青鸾殇` 不同。

---

### 三、章节缺失判断

对每个作品分组：

1. 收集所有已知的**整数章节号**（范围为展开后的数字）。番外、特别篇等非数字不参与缺失判断。  
2. 确定该作品的**最小章节号** `min` 和**最大章节号** `max`（如果无数字章节，则跳过缺失判断）。  
3. 假设作品正常从 1 开始连载（如果 min > 1，则 1 到 min-1 视为缺失）。  
4. 缺失章节 = `[min, max]` 范围内不在已知集合中的整数。  
5. 若已知章节为空（单篇），则不报告缺失。  
6. 若作品存在“番外”标记，单独记录“有番外”但缺失判断不涉及番外。

---

### 四、完本判断规则（优先级从高到低）

1. **任何条目有完本标记** → 该作品完本。  
2. **作品名为单篇无编号**（即只有一条标题且无章节数字） → 视为完本。  
3. **作品标题声称完整范围**（如 `1-8(完)`、`全篇 1-8`、`1-9+番外` 且带“完”字） → 完本。  
4. 若无以上标记，即使章节连续（如 `1,2,3,4,5`）→ **未完结**（因为可能继续出）。  
5. 作品只有一个数字章节（如 `1`）且无完本标记 → 未完结。  
6. 作品有多个章节但存在缺失 → 未完结。

---

### 五、特殊情形处理

- **重复范围**：`1-6` 与 `1-8` 同时出现 → 取并集。  
- **无作者**：按作品名分组，作者留空。  
- **AI绘图、中文标记**：如 `[AI绘图]`、`[Chinese]` 直接忽略，不影响解析。  
- **续集、后记**：若作品名包含“续集”、“后记”且无编号，视为单篇独立作品。  
- **联动篇**：视为单篇作品，完本判断同单篇规则。  
- **缺失的“可能的总章节”**：不主动猜测总章节数，只报告已知范围内的缺失。若已知最大章节后没有结束标记，则后面全部未知（不列出具体缺失数字）。

---

### 六、输出结构示例

```
作者：xxx
- 作品A
  已知章节：1,2,3,5,7-9
  缺失章节：4,6
  是否完本：否
- 作品B（单篇）
  已知章节：单篇
  缺失章节：无
  是否完本：是
```

---

这套规则基于静态模式匹配和启发式判断，不依赖统计或机器学习，可手动实现为脚本或推理引擎。
